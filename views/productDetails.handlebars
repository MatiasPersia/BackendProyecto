<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{product.title}}</title>
</head>
<body>
    <h1>{{product.title}}</h1>
    <p>Descripción: {{product.description}}</p>
    <p>Precio: ${{product.price}}</p>
    <p>Categoría: {{product.category}}</p>
    <form action="/api/carts/:cid/products/{{product._id}}" method="POST">
        <input type="number" name="quantity" min="1" placeholder="Cantidad" required>
        <button type="submit">Agregar al Carrito</button>
    </form>
    {{#if (gt product.price 50)}}
    <p>Este producto es caro.</p>
{{else}}
    <p>Este producto es asequible.</p>
{{/if}}
    <a href="/">Volver a la lista de productos</a>
</body>
</html>
